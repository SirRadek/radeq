Return ONLY a valid JSON object (no prose/fences). RadeQ.cz V3 — Vlna 6: barvy/kontrast/
viditelnost (dle brainstormu A — shoda Gemini+GPT). POUZE CSS (rq.css), namespace `rq-`.
Vracíš edits {file,find,replace} a/nebo rq_css_append. Cíl: ostřejší čitelnost, WCAG AA+,
barevné role disciplinovaně. Nic nerozbít (žádné nové třídy nutné).

## Současné tokeny (src/styles/rq.css :root)
--rq-bg #F7F4EE; --rq-bg-sunken #EFEAE0; --rq-surface #FFFFFF; --rq-ink #1C1A17;
--rq-ink-2 #5A554E; --rq-line #E6E0D4; --rq-burgundy #7A1F2B; --rq-burgundy-strong #5E1622;
--rq-burgundy-tint #F3E7E9; --rq-lime #C7E04A; --rq-lime-ink #2A3508 (lokálně).

## Úpravy (shoda obou providerů)
1. **Sekundární text tmavší (čitelnost):** `--rq-ink-2: #5A554E` → `#4F4942` (bezpečný rozsah,
   ne světlejší než ~#544E46).
2. **Explicitní tokeny pro tmavé sekce (teplý uhlík, ne čistá černá; ne čistě bílý text):**
   přidej do :root: `--rq-carbon: #1A1611; --rq-carbon-2: #242018;
   --rq-on-dark: #F1EDE4; --rq-on-dark-2: #C9BFB1;` a APLIKUJ je na tmavé bloky:
   problem wall (`.rq-problem-wall`/`.rq-statement`) a patičku (`.rq-site-footer`): pozadí
   `--rq-carbon` (karty `--rq-carbon-2`), hlavní text `--rq-on-dark`, sekundární `--rq-on-dark-2`,
   bordery `rgba(247,244,238,.12-.16)`. Najdi jejich aktuální `background`/`color` a přepiš.
   (Pokud teď používají čistě tmavou/bílou, nahraď za tyto tokeny.)
3. **Lime jen diagnostický + VŽDY grafit/olivový text:** zajisti, že KAŽDÝ `.rq-flag` s lime
   pozadím má text `--rq-lime-ink`/`#1C1A17` (nikdy světlý). Pokud existují 2 definice `.rq-flag`,
   sjednoť: lime bg + tmavý text. Lime NEpoužívat jako text na světlém pozadí.
4. **Vínovou používat rozhodně, ale ubrat dekorativní:** ponech vínovou na CTA, odkazy/hover,
   ceny, aktivní stav, doporučený tarif, logo akcent. ZTLUM čistě dekorativní výskyty:
   - hero vínový podkres (radiální wash) sniž opacitu (decentnější),
   - `.rq-h2::after` vínový „tick" zúžit/ztlumit (kratší, nižší opacita) ať nekřičí u každého H2.
   (Neodstraňuj funkční vínovou.)
5. **Kontrast:** ověř, že grafit na ivory zůstává (AAA) a že sekundární text na světlém i
   tmavém splní WCAG AA (≥4.5:1 pro běžný text). Uprav, kde ne.

## Vrať JSON PŘESNĚ:
{
 "edits":[{"file":"src/styles/rq.css","find":"...","replace":"..."}, …],
 "rq_css_append":"(volitelné) další CSS pro dark-section tokeny / lime / burgundy ztlumení",
 "verify_notes":["co Opus zkontroluje (sekundár tmavší, tmavé sekce teplé + čitelné, lime vždy tmavý text, vínová méně dekorativní, AA)"]
}
Bez secrets/logů, žádné staré class names.
