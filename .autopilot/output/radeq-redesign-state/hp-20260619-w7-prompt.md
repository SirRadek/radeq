Return ONLY a valid JSON object (no prose/fences). RadeQ.cz V3 — Vlna 7: typografie,
zalamování, spacing (dle brainstormu A — shoda Gemini+GPT). CSS (rq.css) + 1 úprava hero
markupu (index). Namespace `rq-`. Vracíš edits {file,find,replace} + rq_css_append. Nic nerozbít,
0 overflow, reduced-motion irelevantní (statické).

## Současné (rq.css) — pozn.: pozdější `@appendy` z V2 1a už zvětšily display/h2; tvé NOVÉ
## appendy musí být ZA nimi (vyhrávají). Cílové hodnoty:
1. **Body 18px:** nastav `body { font-size: clamp(1.0625rem, 0.35vw + 1rem, 1.1875rem); line-height: 1.6; }`
   (≈17px mobil → 18px+ desktop). (Aktuálně menší.)
2. **Hero display — zkrátit/zastropovat + ruční zlom:**
   - CSS: `.rq-display { font-size: clamp(2.6rem, 6vw, 6rem); line-height: 1.0; letter-spacing: -0.02em; }`
     (cap ~96px, ne přehnaných ~128px). Na 4K se řeší ve Vlně 8.
   - Markup (index): hero H1 `<h1 class="rq-display">Weby, data a automatizace, které vám uvolní ruce.</h1>`
     → vlož ruční zlom: `Weby, data a automatizace,<br class="rq-br-desktop" /> které vám uvolní ruce.`
     + CSS `.rq-br-desktop{display:none} @media(min-width:768px){.rq-br-desktop{display:inline}}`.
3. **H2 cap:** `.rq-h2 { font-size: clamp(2.1rem, 4.6vw, 3.4rem); line-height: 1.05; }`
4. **Eyebrow 13px:** `.rq-eyebrow { font-size: 0.8125rem; letter-spacing: 0.12em; }`
5. **Zalamování (globálně, bezpečně):**
   - `h1, h2, h3, h4 { text-wrap: balance; }`
   - `p { text-wrap: pretty; }` (globálně OK)
   - max-width řádku NASADIT JEN na čtecí odstavce, NE na karty/flex: `.rq-lead, .rq-prose p,
     .rq-section > p { max-width: 62ch; }` (NEdávej `p{max-width}` plošně — rozbije karty).
6. **Spacing rytmus:**
   - `--rq-pad: clamp(4.5rem, 8vw, 8rem);` (sekce ~72–128px).
   - Mezera eyebrow→H2 ~0.6–0.9rem, H2→lead ~1–1.4rem, nadpis bloku→obsah ~2–3rem
     (uprav přes existující selektory nebo přidej rozumné marginy; nech to vzdušné, ne našlapané).
   - Karty padding ponech rozumný (24–32px), grid gap 20–32px.

## Vrať JSON PŘESNĚ:
{
 "edits":[{"file":"src/pages/index.astro","find":"...","replace":"..."}, … (hero break)],
 "rq_css_append":"CSS append se všemi pravidly výše (body 18px, display/h2 cap, eyebrow 13px, text-wrap balance/pretty, scoped max-width 62ch, --rq-pad, spacing). Za stávající appendy ať vyhraje.",
 "verify_notes":["co Opus zkontroluje (body ~18px, hero čistě zalomený a ne přehnaně velký, balance/pretty, odstavce ~62ch, vzdušnější sekce, 0 overflow, karty nerozbité)"]
}
Bez secrets/logů, žádné staré class names.
