Return ONLY a valid JSON object (no prose/fences). RadeQ.cz V3 — Vlna 8: responzivita do 4K
(dle brainstormu A — shoda Gemini+GPT). CSS (rq.css). Namespace `rq-`. Vracíš edits {file,find,
replace} + rq_css_append. Cíl: na velkých monitorech (1440p/4K) web NEvypadá jako úzká „nudle
v prázdnu" ani roztažený; odstavce zůstanou čitelné (ch limit); 0 horizontal overflow až do 3840px.

## Současné
`--rq-maxw: 1140px;` — používá se přes `width: min(calc(100% - 2rem), var(--rq-maxw))` u sekcí.

## Úpravy (shoda obou providerů)
1. **Zvednout max-width + zavést „wide" token:**
   - `--rq-maxw: 1140px` → `1280px` (textové/obecné sekce, hero, kontakt, jak-pracuji).
   - Přidej `--rq-maxw-wide: 1440px;` a aplikuj ho na vnitřní wrappery „širokých" sekcí, kde
     je víc dlaždic: problem wall, Služby (rq-offers), Ceník (pricing grid), Ukázky teaser,
     diagnostický panel. (Nastav lokálně `--rq-maxw: var(--rq-maxw-wide)` na těchto sekcích nebo
     uprav jejich width.) Odstavce uvnitř ale drž přes `max-width: 62ch` (z Vlny 7).
2. **Full-bleed pozadí:** tmavé bloky (problem wall/statement/footer) a marquee musí jít vždy
   100vw (margin-inline: calc(50% - 50vw)); jen vnitřní obsah omezený max-width. Ověř, že to drží.
3. **Jemný root-rem scale na velkých monitorech** (aby web „povyrostl" proporcionálně, ne aby
   se roztáhly řádky): přidej
   `@media (min-width: 2200px){ html { font-size: 106.25%; } }`
   `@media (min-width: 3200px){ html { font-size: 112.5%; } }`
   (Vše ostatní je v rem → proporcionálně povyroste. Body se tím zvedne mírně — to je OK.)
   NEpoužívej na <1920px (tam zůstává 16px base).
4. **Display strop na 4K:** `.rq-display` clamp horní mez ať nepřeroste cca 6.5rem i s root-scale
   (pokud by kombinace root-scale × clamp dělala přehnaně velký hero na 4K, sniž clamp vw složku
   nebo strop). H2 podobně rozumný strop.
5. **0 overflow:** ověř (a zajisti), že na 1280/1920/2560/3840 nic nepřetéká vodorovně
   (full-bleed přes 100vw nesmí přidat scrollbar — `overflow-x: clip` na html/body už je).

## Vrať JSON PŘESNĚ:
{
 "edits":[{"file":"src/styles/rq.css","find":"...","replace":"..."}, …],
 "rq_css_append":"CSS append: --rq-maxw-wide, root-scale media queries, wide-sekce aplikace, display strop, full-bleed pojistky",
 "verify_notes":["co Opus zkontroluje na 2560/3840 (není nudle, odstavce ~62ch, 0 overflow, hero ne přehnaný)"]
}
Bez secrets/logů, žádné staré class names.
