---
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  lang?: 'cs' | 'en';
  backHref?: string;
  backLabel?: string;
}

const { title, description, lang = 'en', backHref = '/#demos', backLabel = 'Back to projects' } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Radeq.cz demo</title>
    <meta name="description" content={description} />
    <meta name="robots" content="noindex, nofollow" />
  </head>
  <body>
    <main class="demo-route-shell">
      <a class="demo-route-back" href={backHref}>{backLabel}</a>
      <slot />
    </main>
  </body>
</html>
