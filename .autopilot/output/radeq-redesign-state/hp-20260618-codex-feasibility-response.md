{"overall_feasibility":"high","reuse":["src/lib/links.ts withBaseHref() for every internal asset/link that becomes an href/src URL","src/styles/global.css palette tokens, including green accent #257a3e and existing dark theme variables","src/styles/tokens.css display scale, --space-section, and --reveal-duration instead of introducing parallel spacing/motion systems","src/layouts/BaseLayout.astro as the single place already importing global.css and tokens.css","src/components/MotionOrchestrator.tsx existing client:load orchestration, html data-motion-ready, data-motion-scene, and prefers-reduced-motion handling","src/pages/index.astro existing section composition, preserving PricingSection and ContactTerminal.tsx","src/data/siteContent.ts for copy/content changes instead of hardcoding marketing text into components","HeroSection.astro and CoreIsland.tsx as the best place for the one signature visual anchor"],"scroll_reveal_approach":"Extend the existing MotionOrchestrator only if reveal timing needs per-section scene awareness; otherwise add CSS keyed off html[data-motion-ready] main section with initial opacity/transform and use section-level data attributes/classes. Lowest risk: keep MotionOrchestrator as the readiness switch, add CSS reveal rules in global.css/tokens.css, use nth/attribute delays sparingly, and wrap all transitions in prefers-reduced-motion fallbacks. Do not add a second observer or animation runtime unless the current orchestrator cannot expose the needed scene state.","display_font_loading":"Load exactly one Google display font from BaseLayout.astro using preconnect links to https://fonts.googleapis.com and https://fonts.gstatic.com plus a stylesheet URL with display=swap. Define a CSS variable such as --font-display and apply it only to large headline/display selectors, keeping the existing body font as fallback. To reduce layout shift, choose a metrically reasonable font, use font-display: swap from Google, keep line-height explicit, avoid late client-side font injection, and ensure the initial hero headline dimensions are stable with max-width and fixed type scale tokens.","dark_statement_block":"Add one full-width dark statement section using the existing dark theme tokens, preferably as a variant of StudioProof or a new lightweight section inserted between proof/systems and pricing. Keep PricingSection and ContactTerminal outside or visually distinct. Scope the dark styling with a section class/data-theme attribute, reuse existing foreground/accent tokens, and avoid globally toggling the page theme so forms, pricing, and links do not inherit unintended contrast changes.","base_href_risks":["Hero/signature visual images can break on GitHub Pages if src starts with /. Import static assets through Astro when possible or pass public-path strings through withBaseHref().","Any new CTA href, logo link, image src, poster, preload href, or CSS-referenced asset can bypass base=/radeq. Use withBaseHref() for component/template URLs and prefer bundled imports for CSS backgrounds where feasible.","Google Font URLs must remain absolute external URLs and should not go through withBaseHref().","Anchor links like #pricing are safe as fragment-only links, but /#pricing or /contact would break under base=/radeq unless generated through withBaseHref().","React island props that contain asset paths must receive already-normalized URLs from Astro or call the same helper consistently; do not hardcode /assets or /images in CoreIsland.tsx.","Open Graph/canonical URLs should respect site/base behavior separately from local asset hrefs; do not mix withBaseHref() into absolute social metadata unless existing code already does that."],"file_plan":[{"path":"src/layouts/BaseLayout.astro","change":"Add preconnect and one Google display-font stylesheet link, or expose an existing head slot if present; no route logic changes.","risk":"low"},{"path":"src/styles/tokens.css","change":"Add --font-display if missing and tune display headline scale/section rhythm using existing token naming.","risk":"low"},{"path":"src/styles/global.css","change":"Add reveal CSS keyed to html[data-motion-ready], hero display treatment, hover motion, dark statement block styling, and reduced-motion fallbacks.","risk":"med"},{"path":"src/pages/index.astro","change":"Adjust section order/spacing classes and optionally insert one dark statement block while preserving PricingSection and ContactTerminal.tsx.","risk":"med"},{"path":"src/components/HeroSection.astro","change":"Upgrade headline layout, use the display font token, and add the signature visual anchor using withBaseHref() or imported assets.","risk":"med"},{"path":"src/components/CoreIsland.tsx","change":"If the visual anchor is interactive, keep it self-contained, reduced-motion aware, and avoid hardcoded asset roots.","risk":"med"},{"path":"src/components/StudioProof.astro","change":"Candidate location for the one dark statement variant if reusing existing proof content is cleaner than creating a new component.","risk":"low"},{"path":"src/data/siteContent.ts","change":"Adjust homepage copy/proof labels for the craftsman's precision direction without moving content into layout files.","risk":"low"},{"path":"src/lib/links.ts","change":"No expected change; use existing withBaseHref() as-is unless current helper lacks a needed typed variant.","risk":"low"}],"build_verify_steps":["npm install if dependencies are not already present","npm run build","npm run preview -- --host 127.0.0.1","Run a local static preview configured with base=/ if that is the repo default and manually check / loads","Run or simulate the GitHub Pages preview build with base=/radeq and site=https://sirradek.github.io using the existing project script/config","Open the built preview at /radeq/ and verify hero assets, fonts, CSS, section anchors, pricing, and inquiry form render correctly","Run any existing lint/typecheck script if available","Use browser responsive checks for mobile, tablet, and desktop, including prefers-reduced-motion"],"gotchas":["Astro static output means any client-only measurement should not be required for first paint or layout correctness.","Do not add a second scroll orchestration system that races MotionOrchestrator's html data attributes.","CSS background-image: url('/...') is an easy way to break Pages preview under /radeq.","Do not remove or visually bury pricing and ContactTerminal; the redesign goal explicitly requires both.","Google font loading belongs in server-rendered head markup, not in a React island or runtime effect.","Dark block contrast must be checked against existing link/accent tokens, especially #257a3e on dark backgrounds.","React island assets and Astro template assets may resolve differently; normalize URLs at the boundary.","Avoid turning the page into a lepshee clone; keep Czech-SMB trust signals, readable service proof, and conservative conversion structure.","If using Astro public assets, remember public-root paths still need base handling in rendered URLs.","Motion should be decorative enhancement only; content order and form usability must work with JavaScript disabled."]}