Return ONLY a valid JSON object (no prose/fences). KROK V1-5 — VZOROVÁ UKÁZKA: jednoduchý
E-SHOP. Jeden soubor `src/pages/ukazky/eshop.astro` (route /ukazky/eshop/). Modelový e-shop
lokální pražírny kávy — má vypadat jako její obchod, NE jako RadeQ. Lehká ukázka. Česky, SEO,
mobil, rychlé. Drž to JEDNODUCHÉ (záměrně malý e-shop, ne plný e-commerce).

## Rámec (jako u předchozích ukázek)
- `import BaseLayout from '../../layouts/BaseLayout.astro'`; `const base = import.meta.env.BASE_URL.replace(/\/$/, '');`
- Sticky DEMO banner: „Vzorová ukázka studia RadeQ — takhle může vypadat váš e-shop."
  + „← Zpět na RadeQ.cz" href={base + "/"}.
- Vlastní vizuál: teplé kávové tóny (hnědá/krémová), útulné, kvalitní. Astro scoped `<style>`.
  Foto = placeholder bloky (žádné externí obrázky/iframe).

## Obsah (fiktivní „Pražírna U Mlýna")
- Hero: krátký, H1 „Čerstvě pražená káva z malé pražírny." + CTA „Prohlédnout kávu" (#katalog).
- Katalog (id=katalog): mřížka ~6–8 produktů (zrnková/mletá káva, dárkové balení). Karta:
  název, krátký chuťový profil, gramáž, cena, tlačítko „Do košíku". Nad mřížkou jednoduchý
  FILTR (tlačítka: vše / espresso / filtr / bezkofeinová) — funkční přes malý vanilla JS
  (show/hide podle data-atributu). Žádný framework.
- Detail produktu: NEdělej samostatné stránky; stačí, že karty mají variantu gramáže
  (250 g / 500 g / 1 kg) jako `<select>` měnící zobrazenou cenu (malý vanilla JS).
- KOŠÍK: tlačítko „Do košíku" přidá položku do jednoduchého drawer košíku (vyjede zprava),
  ukáže položky + mezisoučet. Čistě client-side (vanilla JS, in-memory). „Do košíku" po
  kliknutí krátce ukáže stav „Přidáno ✓".
- Pokladna: tlačítko „K pokladně" → zobrazí hlášku/box: „Toto je ukázkový e‑shop. V ostré
  verzi by zde byla platební brána a doprava (Zásilkovna, kurýr, osobní odběr)." (žádný backend)
- Co e-shop NEcpe: účty/přihlášení, reálné platby, slevové kódy, recenze s hvězdičkami.
- Důvěra: jasná dostupnost „skladem", doprava/odběr orientačně v textu.
- Patička: pražírna, kontakt.

## Požadavky
- JS čistě vanilla, malý, v `<script>` na konci (žádné importy frameworků). Funkční filtr,
  varianta ceny, drawer košík, hláška u pokladny.
- Vše scoped, žádné staré class names projektu, žádné externí závislosti/iframe/obrázky.
- Přístupné (aria pro drawer, tlačítka), 0 horizontal overflow, mobil OK, rychlé.

## Vrať JSON PŘESNĚ:
{
 "eshop_astro":"FULL src/pages/ukazky/eshop.astro (BaseLayout + demo banner + scoped <style> + katalog/filtr/varianta/drawer košík/hláška + <script> vanilla JS)",
 "verify_notes":["co zkontrolovat"]
}
Bez secrets/logů.
