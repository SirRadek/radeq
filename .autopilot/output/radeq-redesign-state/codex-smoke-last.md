Created `.autopilot-write-test.txt` at the repo root with exactly `OK-WRITE-MODE`.

`git status --short` output:

```text
?? .autopilot-write-test.txt
?? scripts/preview-shot.mjs
?? scripts/verify-motion.mjs
```

I did not run `git add`, commit, or push.