Return ONLY a valid JSON object (no prose/fences). KROK V1-1 čisté přestavby radeq.cz
(Astro + Tailwind v4). Výstup aplikuje supervisor doslova. Cíl: ČISTÝ základ s NOVÝM
namespace `rq-` — NESMÍŠ použít žádné staré class names z global.css (hero-section,
hero-copy, command-header, service-catalog, section-heading, about-services, meta-row,
pricing-section, terminal-section, brand-mark…). Nový web nesmí dědit nic ze starého.

## Koncept (fixní)
RadeQ.cz — „Racionální digitalizace". Jeden technik s okem analytika a precizností testera:
postaví, změří, předá, nezmizí. Cílovka: živnostníci/malé firmy/spolky (konzervativní CZ
SMB). Tón: preciznost + lehká hravost. Cenová hladina LEVNĚJŠÍ (od…). SEO + výkon 100/100.
**Barva: vínová (burgundy) STŘÍDMĚ ~5 %** (CTA, hover, akcent), jinak světlá ivory báze
+ grafit. V1 je SVĚTLÁ verze — NEpoužívej staré přepínače stylu/témat (StyleVariantToggle,
ThemeModeToggle) ani CoreIsland (kočku). Méně cruftu.

## Existující fakta
- BaseLayout.astro importuje jen `'../styles/global.css'`. `<html data-style="variant-a"
  data-theme="light">`. V `<body>`: `<MeasurementTracker client:load/>`,
  `<MotionOrchestrator client:load/>` (nastavuje `html[data-motion-ready]` na `main section`),
  `<slot/>`. Toto zachovej.
- Logo: `public/brand/radeq-logo-b-mask.png` = maska celé wordmarky „RadeQ.cz" (aspect ~920/164).
  Recolor přes CSS mask: wordmark = `--rq-ink`, ocásek Q = `--rq-burgundy`.
- Asset URL přes `import.meta.env.BASE_URL` (pozor na base=/radeq). Font URL absolutní.

## Co vytvořit
### 1) `src/styles/rq.css` — čistý design systém
Tokeny na :root: --rq-bg:#F7F4EE; --rq-bg-sunken:#EFEAE0; --rq-surface:#FFFFFF;
--rq-ink:#1C1A17; --rq-ink-2:#5A554E; --rq-line:#E6E0D4; --rq-burgundy:#7A1F2B;
--rq-burgundy-strong:#5E1622; --rq-burgundy-tint:#F3E7E9; --rq-maxw:1140px;
--rq-pad:clamp(4rem,8vw,7.5rem); --rq-r:14px; --rq-r-pill:999px; --rq-ease:cubic-bezier(.16,1,.3,1).
Fonts: --rq-display:"Plus Jakarta Sans"; --rq-body:"Inter"; --rq-mono:"JetBrains Mono".
NEUTRALIZUJ starý canvas: `html,body{background:var(--rq-bg)!important;overflow-x:clip}` a
`body::before,body::after{display:none!important}`. Body font Inter, h1/h2/h3 Plus Jakarta 800,
tracking -0.02em. Primitiva: `.rq-section`(width min(100%-2rem,var(--rq-maxw));margin-inline:auto;
padding-block:var(--rq-pad)), `.rq-bleed`(width:100vw;margin-inline:calc(50% - 50vw)),
`.rq-eyebrow`(mono,.72rem,uppercase,letter-spacing .14em,color var(--rq-burgundy),leading „—"),
`.rq-display`(clamp(2.8rem,6vw,4.8rem)), `.rq-h2`(clamp(2rem,4.4vw,3.3rem)),
`.rq-lead`(clamp(1.05rem,1.5vw,1.22rem),color var(--rq-ink-2),max-width 54ch),
`.rq-card`(surface,1px var(--rq-line),radius,hover lift -4px + shadow),
`.rq-btn`(pill, Inter 600), `.rq-btn--primary`(bg var(--rq-burgundy),#fff, ::after „→" posun 4px na hover, hover bg strong),
`.rq-btn--ghost`(transparent,1px var(--rq-line),ink, hover border burgundy).
`.rq-logo`{display:block;width:clamp(8.5rem,13vw,11rem);aspect-ratio:920/164;
 -webkit-mask:var(--rq-logo-mask) center/contain no-repeat;mask:var(--rq-logo-mask) center/contain no-repeat;
 background:linear-gradient(90deg,var(--rq-ink) 0 58%,var(--rq-burgundy) 58% 79%,var(--rq-ink) 79% 100%);}
Reveal přes html[data-motion-ready="true"] (opacity/translateY, @starting-style), reduced-motion safe.
Vše prefixuj `rq-`.

### 2) `src/components/RqHeader.astro`
Props: `{ content: { navItems:{href,label}[]; cta:string }; alternateLabel?:string; alternatePath?:string }`.
Sticky, translucent var(--rq-bg) + blur, spodní 1px linka. Vlevo `<a class="rq-logo" .. style={`--rq-logo-mask:url("${base}/brand/radeq-logo-b-mask.png")`} aria-label="RadeQ.cz">` (prázdný span, role img). Uprostřed nav (rq- třídy) z navItems. Vpravo EN odkaz (pokud je) + `.rq-btn rq-btn--primary` malé CTA href="#kontakt". Mobile <860px: skryj nav. ŽÁDNÉ toggle islandy.

### 3) `src/components/RqFooter.astro`
Props: `{ navItems:{href,label}[] }`. Full-bleed (.rq-bleed) tmavá vínovo-grafitová (#201A1B nebo color-mix), světlý text, sloupce: logo (světlá varianta — `background:#F7F4EE` na masce) + tagline, Navigace (navItems), Spojení (#kontakt „Napsat poptávku", #ceny „Ceník"). Dole „© 2026 RadeQ.cz". Vínové hover.

### 4) `src/pages/index.astro` (NAHRADIT) — čistá homepage, jen základ V1-1
Import BaseLayout, RqHeader, RqFooter. `const nav=[{href:'#co-resim',label:'Co řeším'},{href:'#sluzby',label:'Služby'},{href:'#jak-pracuji',label:'Jak pracuji'},{href:'#ceny',label:'Ceny'},{href:'/ukazky/',label:'Ukázky'},{href:'#kontakt',label:'Kontakt'}];`
Struktura: `<RqHeader content={{navItems:nav,cta:'Chci zmapovat problém'}} alternateLabel="EN" alternatePath="/en/" />`,
`<main>` → HERO sekce (`.rq-section`, id="top"):
  - `.rq-eyebrow`: „Racionální digitalizace"
  - H1 `.rq-display`: „Weby, data a automatizace, které vám uvolní ruce."
  - `.rq-lead`: „Jeden technik s okem analytika a precizností testera. Navrhnu, postavím, otestuji a předám tak, že v tom nezůstanete sami."
  - proof rail (3, hairline-top, mono label + text): „Web od 9 900 Kč" / „rychlý web, co vodí poptávky" · „Audit od 4 900 Kč" / „zmapuju, co brzdí, odečtu z realizace" · „Postavím · změřím · předám" / „bez závislosti na dodavateli"
  - akce: `.rq-btn rq-btn--primary` „Chci zmapovat problém" href="#kontakt" + `.rq-btn rq-btn--ghost` „Co umím zjednodušit" href="#sluzby"
  → pak prozatímní `<section class="rq-section" id="kontakt">` placeholder s H2 „Napište, co vás pálí" + odstavec + `.rq-btn--primary` „Napsat poptávku" (mailto nebo #).
`<RqFooter navItems={nav} />` za `</main>`.
Žádné staré komponenty/islandy (CommandHeader, HeroSection, CoreIsland, ContactTerminal) v index.

### 5) BaseLayout patch
- import `'../styles/rq.css'` PO global.css.
- do <head> přidat preconnect + `https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@700;800&display=swap`.
- favicon: `<link rel="icon" href={base + '/favicon.svg'} type="image/svg+xml">`.

## Vrať JSON PŘESNĚ tyto klíče
{
 "rq_css":"FULL src/styles/rq.css",
 "rqheader_astro":"FULL src/components/RqHeader.astro",
 "rqfooter_astro":"FULL src/components/RqFooter.astro",
 "index_astro":"FULL nový src/pages/index.astro",
 "baselayout_import_line":"import '../styles/rq.css';",
 "baselayout_head_html":"<link> tagy (preconnect+fonts+favicon) k vložení do <head>",
 "verify_notes":["co zkontrolovat po buildu"]
}
Žádné staré class names. Base-aware asset URL. Žádné secrets/logy.
