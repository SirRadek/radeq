Return ONLY a valid JSON object (no prose/fences). RadeQ.cz V2 — Vlna 4 (doladění): (A)
„marquee symptomů" — tenký běžící pás mezi hero a sekcí #co-resim; (B) zvýraznění existující
„Blueprint" SVG linky v #jak-pracuji (teď je moc decentní). Namespace `rq-`. CSS-driven,
reduced-motion + no-JS safe, 0 horizontal overflow.

## (A) Marquee symptomů
- Tenký full-bleed pás (.rq-bleed) na tmavém nebo sunken pozadí, přes celou šířku, mezi hero
  a #co-resim. Plynule běžící text (CSS @keyframes translateX, GPU), **pauza na hover**,
  `prefers-reduced-motion` → běh zastaven a obsah čitelný (zalomený/posuvný staticky).
- Obsah = krátké reálné symptomy oddělené např. vínovou tečkou „•":
  „pomalý web • poptávky se ztrácí v mailu • ruční přepisování do Excelu • nejasné CTA •
  web nevodí zákazníky • žádné měření • chaos v nástrojích" (klidně 1–2× zopakuj kvůli plynulosti).
- Mono/uppercase drobné písmo, decentní. Dekorativní, ale text reálný (aria-hidden u duplicit).
- Vrať jako `marquee_html` (vloží se mezi hero a #co-resim) + CSS v `rq_css_append`.
  (Pokud rq.css už má `.rq-marquee` hook z Vlny 1a, navaž na něj / dolaď.)

## (B) Blueprint linka výraznější
- V #jak-pracuji je `<svg class="rq-process__line"><path class="rq-process__line-path" …>`.
  Zvýši viditelnost: silnější stroke-width, vyšší opacita/kontrast vůči pozadí, případně
  jemný **pohybující se bod** (small circle) putující po path při reveal (offset-path nebo
  animace), vínový. Reduced-motion: bez pohybu, ale linka plně viditelná. Stále dekorativní
  (aria-hidden), nepřekrývat text, mobil ok.
- Vrať jen CSS úpravy (v `rq_css_append`); pokud potřebuješ přidat 1 element do SVG (bod),
  vrať i `blueprint_dot_html` = přesný `<circle …>` k vložení do existujícího path SVG
  + `blueprint_insert_after` = řetězec v markupu, za který ho vložit.

## Vrať JSON PŘESNĚ:
{
 "marquee_html":"<section/div> pásu symptomů (vloží se mezi hero a #co-resim)",
 "rq_css_append":"CSS: marquee (běh, pauza na hover, reduced-motion stop) + zvýraznění blueprint linky. rq- prefix, 0 overflow.",
 "blueprint_dot_html":"(volitelné) <circle …/> nebo prázdný řetězec",
 "blueprint_insert_after":"(volitelné) řetězec za který vložit bod, nebo prázdný",
 "verify_notes":["co Opus zkontroluje (marquee běží+pauza+reduced-motion, blueprint výraznější, 0 overflow, mobil)"]
}
Bez secrets/logů, žádné staré class names.
