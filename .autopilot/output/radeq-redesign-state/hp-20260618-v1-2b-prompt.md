Return ONLY a valid JSON object (no prose/fences). KROK V1-2b — další homepage sekce
radeq.cz. Namespace `rq-` (NE staré class names). Reuse rq.css primitiva (.rq-section,
.rq-bleed, .rq-card, .rq-eyebrow, .rq-h2, .rq-lead, .rq-btn/--primary/--ghost; tokeny
--rq-bg,--rq-bg-sunken,--rq-surface,--rq-ink,--rq-ink-2,--rq-line,--rq-burgundy,
--rq-burgundy-strong,--rq-burgundy-tint,--rq-r,--rq-r-pill,--rq-mono,--rq-display).
Česky, SEO, levnější hladina. Reduced-motion safe, 0 overflow.

## Vracíš 3 věci
### A) „Ceny" sekce id="ceny" — předplatné (péče po spuštění)
eyebrow „Ceny", .rq-h2 „Orientační ceny bez překvapení.", krátký .rq-lead. 3 karty tarifů
(.rq-card; prostřední featured — vínový rámeček/štítek „Doporučeno"):
- Klid — 2 500 Kč/měs. — „provoz, zálohy, drobné úpravy (1 h), máte komu zavolat"
- Růst — 6 000 Kč/měs. — „vše z Klidu + SEO/výkon hlídání, měsíční report, 3 h vývoje" (featured)
- Partner — 15 000 Kč/měs. — „vše z Růstu + průběžná automatizace/AI, 10 h, prioritní support"
Pod kartami pozn.: „Nechcete paušál? Bloky hodin 5/15/40 h (od 1 100 Kč/h). Audit lze
odečíst z realizace." Každá karta CTA .rq-btn--ghost „Mám zájem" href="#kontakt"
(featured .rq-btn--primary).

### B) „Ukázky" teaser sekce id="ukazky-teaser"
eyebrow „Ukázky", .rq-h2 „Co umím postavit", 3 karty .rq-card se štítkem `VZOROVÝ PROJEKT`
(malý vínový badge). Odkazy MUSÍ být base‑aware přes Astro výraz `{base + "..."}` (proměnná
`base` BUDE definovaná ve frontmatteru index.astro):
- „Web pro instalatéra" — „lokální SEO + telefonní poptávky" — href={base + "/ukazky/instalater/"}
- „Web s rezervací" — „objednání termínu + budování důvěry" — href={base + "/ukazky/sluzba/"}
- „Malý e‑shop" — „čistý katalog + košík bez balastu" — href={base + "/ukazky/eshop/"}
(na --rq-bg-sunken jako full‑bleed pruh přes element bg + vnitřní wrapper)

### C) „Kontakt" — NAHRADÍ stávající placeholder sekci id="kontakt" reálným formulářem
eyebrow „Kontakt", .rq-h2 „Napište, co vás pálí", krátký .rq-lead. Čistý přístupný `<form
method="post" action={base + "/api/lead"}>`: pole Jméno (text, required), E‑mail (email,
required), select „Co dnes používáte?" (Web/E‑mail/Tabulky/Formulář/Jiné), „Co potřebujete
zjednodušit?" (textarea, required), submit .rq-btn--primary „Odeslat poptávku". Vysoký
kontrast: bílá pole (--rq-surface), 1px --rq-line, focus vínový ring (box-shadow), Inter
text. Pod formulářem drobně: „Nebo napište na info@radeq.cz." Pole label+input standardně.

## Vrať JSON PŘESNĚ:
{
 "ceny_ukazky_html":"markup 2 <section> bloků (ceny + ukazky-teaser) v pořadí — vloží se před #kontakt; smí používat {base + ...} výrazy",
 "kontakt_html":"FULL nová <section ... id=\"kontakt\">…</section> s formulářem (nahradí placeholder)",
 "rq_css_append":"CSS append pro ceny (tarify+featured), ukazky teaser (badge), kontakt formulář (pole+focus ring). rq- prefix.",
 "verify_notes":["co zkontrolovat"]
}
Žádné staré class names, bez secrets/logů.
